import { FilterHasExpertPipe } from './filter-has-expert-pipe';

describe('FilterHasExpertPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHasExpertPipe();
    expect(pipe).toBeTruthy();
  });
});
