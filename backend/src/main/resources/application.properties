server.port=8082
spring.datasource.url=jdbc:mysql://localhost:3306/SalamaInsuranceDB?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.max-http-request-header-size=2000000

# Pour Spring Boot 4.0.2
#spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.SecurityAutoConfiguration

springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs

app.upload.dir=uploads/photos-accident
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB


# Configuration Gmail SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=adsalamainsurance@gmail.com
spring.mail.password=kaxy mktt athz nxzf
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate=DEBUG
logging.level.org.example.salamainsurance=DEBUG


# Gemini API
gemini.api.key=${GEMINI_API_KEY:}
# Configuration supplémentaire
spring.main.allow-circular-references=true
logging.level.org.springframework.web.client=DEBUG

# WebSocket
spring.websocket.enabled=true

# Pour servir les fichiers statiques du dossier chatbot
spring.web.resources.static-locations=classpath:/static/,classpath:/public/,classpath:/resources/,classpath:/META-INF/resources/,classpath:/chatbot/

# Thymeleaf configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=false

# Hugging Face API (gratuit)
huggingface.api.key=${HUGGINGFACE_API_KEY:}

# JWT
jwt.secret=SalamaInsuranceSecretKeyForJWTTokenGenerationMustBeAtLeast256BitsLongForHS256
jwt.expiration-ms=86400000

# Verification token
verification.token.expiry-hours=24

# Password reset token
password.reset.token.expiry-minutes=30

# Mail envelope (EmailService). Defaults align with spring.mail; override with MAIL_FROM / MAIL_FROM_NAME if needed.
mail.from=${MAIL_FROM:${spring.mail.username}}
mail.from-name=${MAIL_FROM_NAME:Salama Insurance}
# Public base URL for links in emails (verification, etc.). Override with APP_BASE_URL when not localhost.
app.base-url=${APP_BASE_URL:http://localhost:8082}

# Public base URL for password reset page (should be a frontend URL).
# Contains ONLY the raw token in the query string. Override with PASSWORD_RESET_BASE_URL.
password.reset.base-url=${PASSWORD_RESET_BASE_URL:http://localhost:4200/reset-password}

# Google OAuth2
spring.security.oauth2.client.registration.google.client-id=176526393609-56fcp5b7lpfdboub6fhvh0gm0acc7c35.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope=openid,profile,email
spring.security.oauth2.client.registration.google.redirect-uri=${GOOGLE_REDIRECT_URI:http://localhost:8082/login/oauth2/code/google}

spring.security.oauth2.client.registration.github.client-id=Ov23lixdBCpHEYyonHJl
spring.security.oauth2.client.registration.github.client-secret=36d5ba8183f9e237a978cc0a73667af8f6cc146e

spring.security.oauth2.client.registration.github.scope=read:user,user:email

spring.security.oauth2.client.provider.github.user-name-attribute=login

